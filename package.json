{
    "name": "flyginfo",
    "description": "Departure and arrival times for Swedavia's airports",
    "version": "1.0.0",
    "engines": {
      "node": "14.x",
      "yarn": "1.22.10"
    },
    "scripts": {
        "build": "cd frontend/ && yarn && yarn build && cp -r build/ ../backend/public && cd ../backend/ && yarn",
        "start": "cd backend/ && yarn start"
    }
  }
